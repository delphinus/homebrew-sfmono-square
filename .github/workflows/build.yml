---
name: Build check
on:
  push:
  schedule:
    - cron: '25 */3 * * *'
jobs:
  build:
    runs-on: macos-11
    timeout-minutes: 15
    steps:
      - uses: Homebrew/actions/setup-homebrew@master
      - uses: actions/checkout@v2
      - run: bin/github-build.sh
